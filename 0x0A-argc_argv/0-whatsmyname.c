#include<stdlib.h>
#include<stdio.h>
/**
 * main - program prints its name, followed by a new line
 * @argc: number of arguments to be counted on the program
 * @argv: lists of arguments passed on the function
 * Return: Always 0
 */
int main(int argc, char **argv)
{
	(void)argc;
	printf("%s\n", argv[0])
	return (0);
}	
